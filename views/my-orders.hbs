{{> header}}

<section class="section-myorders">

  <div class="fullscreen-bg"></div>

  <div class="row justify-content-center">

    <div class="div1">

  <img src="/images/icon-bottles.svg" alt="" class="icon">
  <div class="h6">My orders</div>

  {{> flash_messages}}

  <div class="orderlist col-lg-12 col-md-12">
   {{#each orderInfo}}
      <div class="wineinfos col-lg-12 col-md-12">
      <div class="h5">Order n° {{this.randomId}}</div>
      <div class="h4">{{this.createdAt}}</div>
        <ul class="winelist">
              {{#each this.cart}}
              <li class="h6">{{this.appelation}}</li>
              {{/each}}
        </ul>
        <div class="h5">{{this.hourOrdered}}</div>
        <div class="h2">{{this.shippingAddress}}</div>
      </div>
   {{/each}}
  </div>

  </div>
  
  <a href="/profile"><button class="button-font">back</button></a>

</section>

{{> footer}}