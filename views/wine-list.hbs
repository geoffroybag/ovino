{{> header}}

<section class="section-winelist">

  <h2>Wine list:</h2>
  <ul class="list">

  {{#each allWines}}
    <li class="one-wine">
      <div class="img">
        <img src="{{this.pictureUrl}}" alt="">
      </div>
      <div class="appelation">{{this.appelation}}</div>
      <ul class="proprieties">
        <li>{{this.domaine}} - {{this.millesime}}</li>
      </ul>
  </li>
  {{/each}}
  </ul>

</section>